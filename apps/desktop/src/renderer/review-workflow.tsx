import { useEffect, useMemo, useRef, useState, type JSX } from 'react';

import type { WorkspaceCollaborationDto, WorkspaceProblemDto } from '@abnt/protocol';

import { codeActionsForProblem, type DiagnosticWorkspaceEdit } from './shell/diagnostic-actions.js';
import type { EditorViewState } from './shell/views.js';
import { useDialogAccessibility } from './dialog-accessibility.js';
import { VirtualizedList } from './virtualized-list.js';
import { requestText } from './text-prompt.js';

export interface ReviewReply { readonly id: string; readonly message: string; readonly createdAt: number; }
export interface ReviewComment {
  readonly id: string; readonly fileId: string; readonly path: string; readonly revision: number;
  readonly range: { readonly start: number; readonly end: number }; readonly message: string;
  readonly createdAt: number; readonly resolvedAt?: number; readonly replies?: readonly ReviewReply[];
}
type ReviewFilters = { readonly severity: 'all' | 'error' | 'warning' | 'info'; readonly category: 'all' | 'citations' | 'bibliography' | 'figures'; readonly scope: 'workspace' | 'document'; };
const filtersKey = (workspaceId: string): string => `folio.review.filters:${workspaceId}`;
const commentsKey = (workspaceId: string): string => `folio.review.comments:${workspaceId}`;
const defaults: ReviewFilters = { severity: 'all', category: 'all', scope: 'workspace' };

export const readReviewComments = (workspaceId: string): readonly ReviewComment[] => {
  try { const value: unknown = JSON.parse(window.localStorage.getItem(commentsKey(workspaceId)) ?? '[]'); return Array.isArray(value) ? value.filter((entry): entry is ReviewComment => typeof entry === 'object' && entry !== null && typeof entry.id === 'string' && typeof entry.fileId === 'string' && typeof entry.path === 'string' && typeof entry.revision === 'number' && typeof entry.message === 'string' && typeof entry.createdAt === 'number' && typeof (entry as { range?: unknown }).range === 'object') : []; } catch { return []; }
};
export const writeReviewComments = (workspaceId: string, comments: readonly ReviewComment[]): void => { try { window.localStorage.setItem(commentsKey(workspaceId), JSON.stringify(comments)); } catch { /* cache local opcional */ } };
const categoryFor = (problem: WorkspaceProblemDto): ReviewFilters['category'] => problem.ruleId.startsWith('CIT') ? 'citations' : problem.ruleId.startsWith('REF') ? 'bibliography' : problem.ruleId.startsWith('FIG') || problem.ruleId.startsWith('TAB') ? 'figures' : 'all';

const fromShared = (comments: readonly NonNullable<WorkspaceCollaborationDto['comments']>[number][]): readonly ReviewComment[] => comments.map((comment) => ({ id: comment.id, fileId: comment.fileId, path: comment.path, revision: comment.revision, range: comment.range, message: comment.message, createdAt: comment.createdAt, ...(comment.resolvedAt === undefined ? {} : { resolvedAt: comment.resolvedAt }), ...(comment.replies === undefined ? {} : { replies: comment.replies.map((reply) => ({ id: reply.id, message: reply.message, createdAt: reply.createdAt })) }) }));

export function ReviewWorkspaceDialog({ workspaceId, activeView, onClose, onOpenProblem, onApplyEdit }: {
  readonly workspaceId: string; readonly activeView: EditorViewState | undefined; readonly onClose: () => void;
  readonly onOpenProblem: (problem: WorkspaceProblemDto) => void; readonly onApplyEdit: (edit: DiagnosticWorkspaceEdit) => void;
}): JSX.Element {
  const dialog = useRef<HTMLElement>(null); const collaboration = useRef<WorkspaceCollaborationDto | undefined>(undefined);
  const [problems, setProblems] = useState<readonly WorkspaceProblemDto[]>([]);
  const [comments, setComments] = useState<readonly ReviewComment[]>(() => readReviewComments(workspaceId));
  const [filters, setFilters] = useState<ReviewFilters>(() => { try { return { ...defaults, ...(JSON.parse(window.localStorage.getItem(filtersKey(workspaceId)) ?? '{}') as Partial<ReviewFilters>) }; } catch { return defaults; } });
  const [commentText, setCommentText] = useState('');
  useDialogAccessibility(dialog, onClose);
  useEffect(() => { let cancelled = false; void window.academic.workspace.problems({}).then((result) => { if (!cancelled && result.ok) setProblems(result.value); }); return () => { cancelled = true; }; }, [workspaceId]);
  useEffect(() => { let cancelled = false; void window.academic.workspace.collaboration().then((result) => { if (!cancelled && result.ok) { collaboration.current = result.value; if (result.value.comments !== undefined) { const shared = fromShared(result.value.comments); setComments(shared); writeReviewComments(workspaceId, shared); } } }); return () => { cancelled = true; }; }, [workspaceId]);
  useEffect(() => { try { window.localStorage.setItem(filtersKey(workspaceId), JSON.stringify(filters)); } catch { /* preferência local opcional */ } }, [filters, workspaceId]);
  const shown = useMemo(() => problems.filter((problem) => (filters.severity === 'all' || problem.severity === filters.severity) && (filters.category === 'all' || categoryFor(problem) === filters.category) && (filters.scope === 'workspace' || problem.fileId === activeView?.fileId)), [problems, filters, activeView?.fileId]);
  const persist = (next: readonly ReviewComment[]): void => { writeReviewComments(workspaceId, next); const current = collaboration.current; if (current === undefined) return; void window.academic.workspace.setCollaboration({ ...current, comments: next }).then((result) => { if (result.ok) collaboration.current = result.value; }); };
  const updateComments = (next: readonly ReviewComment[]): void => { setComments(next); persist(next); };
  const addComment = (): void => { if (activeView === undefined || commentText.trim() === '') return; const range = { start: Math.min(activeView.snapshot.selection.anchor, activeView.snapshot.selection.head), end: Math.max(activeView.snapshot.selection.anchor, activeView.snapshot.selection.head) }; updateComments([...comments, { id: crypto.randomUUID(), fileId: activeView.fileId, path: activeView.path, revision: activeView.snapshot.session.revision, range, message: commentText.trim(), createdAt: Date.now() }]); setCommentText(''); };
  const replyTo = async (comment: ReviewComment): Promise<void> => { const message = await requestText('Responder ao comentário'); if (message === null || message.trim() === '') return; updateComments(comments.map((item) => item.id === comment.id ? { ...item, replies: [...(item.replies ?? []), { id: crypto.randomUUID(), message: message.trim(), createdAt: Date.now() }] } : item)); };
  const toggleResolved = (comment: ReviewComment): void => updateComments(comments.map((item) => { if (item.id !== comment.id) return item; if (item.resolvedAt === undefined) return { ...item, resolvedAt: Date.now() }; const { resolvedAt: _resolvedAt, ...open } = item; return open; }));
  const activeProblem = (problem: WorkspaceProblemDto): ReturnType<typeof codeActionsForProblem> => problem.fileId === activeView?.fileId && problem.revision === activeView.snapshot.session.revision ? codeActionsForProblem(problem, activeView.snapshot.session.content) : [];
  return <div className="fixed inset-0 z-[56] grid place-items-center bg-slate-950/45 p-5"><section ref={dialog} role="dialog" aria-modal="true" aria-labelledby="review-title" className="flex h-[min(84vh,54rem)] w-full max-w-6xl flex-col overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-2xl"><header className="flex border-b border-slate-200 px-6 py-4"><div><h2 id="review-title" className="text-lg font-bold text-slate-900">Modo de revisão</h2><p className="text-sm text-slate-500">Problemas, comentários e correções seguras na mesma superfície.</p></div><button type="button" aria-label="Fechar modo de revisão" className="ml-auto text-xl text-slate-500" onClick={onClose}>×</button></header><div className="flex flex-wrap gap-2 border-b border-slate-100 px-6 py-3 text-xs">{(['all', 'error', 'warning', 'info'] as const).map((value) => <button key={value} type="button" onClick={() => setFilters((current) => ({ ...current, severity: value }))} className={`rounded-full px-2 py-1 ${filters.severity === value ? 'bg-indigo-600 text-white' : 'bg-slate-100 text-slate-600'}`}>{value === 'all' ? 'Todos' : value}</button>)}{(['all', 'citations', 'bibliography', 'figures'] as const).map((value) => <button key={value} type="button" onClick={() => setFilters((current) => ({ ...current, category: value }))} className={`rounded-full px-2 py-1 ${filters.category === value ? 'bg-indigo-600 text-white' : 'bg-slate-100 text-slate-600'}`}>{value === 'all' ? 'Tudo' : value}</button>)}<button type="button" onClick={() => setFilters((current) => ({ ...current, scope: current.scope === 'workspace' ? 'document' : 'workspace' }))} className="rounded-full bg-slate-100 px-2 py-1 text-slate-600">{filters.scope === 'workspace' ? 'Vault inteiro' : 'Documento atual'}</button></div><div className="grid min-h-0 flex-1 lg:grid-cols-[minmax(0,1fr)_minmax(19rem,.6fr)]"><div className="flex min-h-0 flex-col p-5"><h3 className="text-sm font-semibold text-slate-800">Problemas ({shown.length})</h3>{shown.length === 0 ? <p className="py-8 text-sm text-slate-500">Nenhum problema neste filtro.</p> : <VirtualizedList ariaLabel="Problemas de revisão" className="mt-2 flex-1 divide-y divide-slate-100" height="100%" viewportHeight={540} itemHeight={116} items={shown} getKey={(problem, index) => `${problem.fileId}:${problem.ruleId}:${problem.range?.start ?? index}`} renderItem={(problem) => <div className="py-3"><button type="button" className="w-full text-left hover:text-indigo-700" onClick={() => onOpenProblem(problem)}><span className="mr-2 text-xs uppercase text-slate-400">{problem.severity}</span><code className="mr-2 text-xs text-slate-500">{problem.ruleId}</code>{problem.message}<span className="ml-2 text-xs text-slate-400">{problem.path}{problem.section === undefined ? '' : ` · ${problem.section}`}</span></button>{activeProblem(problem).map((action) => <button key={action.id} type="button" className="mt-2 rounded bg-indigo-50 px-2 py-1 text-xs font-medium text-indigo-700" onClick={() => onApplyEdit(action.edit)}>{action.title}</button>)}</div>} />}</div><aside className="min-h-0 overflow-auto border-l border-slate-100 bg-slate-50 p-5"><h3 className="text-sm font-semibold text-slate-800">Comentários editoriais</h3><p className="mt-1 text-xs text-slate-500">Estado operacional compartilhável; não entra no Markdown.</p><textarea value={commentText} onChange={(event) => setCommentText(event.target.value)} disabled={activeView === undefined} placeholder="Comentário sobre a seleção atual…" className="mt-3 w-full rounded-lg border border-slate-200 bg-white p-2 text-sm" /><button type="button" disabled={activeView === undefined || commentText.trim() === ''} onClick={addComment} className="mt-2 rounded bg-indigo-600 px-3 py-1.5 text-sm text-white disabled:opacity-40">Adicionar comentário</button><ul className="mt-4 grid gap-2">{comments.map((comment) => <li key={comment.id} className={`rounded-lg border bg-white p-2 text-sm ${comment.resolvedAt === undefined ? 'border-slate-200' : 'border-emerald-200 opacity-75'}`}><div className="text-xs text-slate-400">{comment.path} · rev. {comment.revision}{comment.resolvedAt === undefined ? '' : ' · resolvido'}</div><p className="mt-1 text-slate-700">{comment.message}</p>{(comment.replies ?? []).map((reply) => <p key={reply.id} className="mt-2 border-l-2 border-indigo-200 pl-2 text-xs text-slate-600">{reply.message}</p>)}<div className="mt-2 flex gap-3"><button type="button" className="text-xs text-indigo-700" onClick={() => void replyTo(comment)}>Responder</button><button type="button" className="text-xs text-emerald-700" onClick={() => toggleResolved(comment)}>{comment.resolvedAt === undefined ? 'Resolver' : 'Reabrir'}</button><button type="button" className="text-xs text-red-600" onClick={() => updateComments(comments.filter((item) => item.id !== comment.id))}>Remover</button></div></li>)}</ul></aside></div></section></div>;
}
