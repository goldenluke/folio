import { describe, expect, it } from 'vitest';
import { claimReadiness, type EvidenceSynthesis } from '@abnt/evidence-synthesis';
const base: EvidenceSynthesis = { version: 1, sources: [], strategies: [], runs: [], inbox: [], records: [], works: [{ id: 'w', title: 'W', recordIds: [], artifactIds: [], fullText: 'available' }], artifacts: [], evidenceItems: [{ id: 'c', workId: 'w', label: 'C', kind: 'claim' }, { id: 'e', workId: 'w', label: 'E', kind: 'finding' }], stages: [], criteria: [], decisions: [], extractions: [], claimRelations: [{ id: 'r', claimId: 'c', evidenceItemId: 'e', relation: 'supports', reviewerId: 'local', createdAt: 'now' }] };
describe('CI.9 readiness', () => { it('exige extração para liberar a claim', () => { expect(claimReadiness(base, 'c').ready).toBe(false); expect(claimReadiness({ ...base, extractions: [{ id: 'x', evidenceItemId: 'e', fieldId: 'result', value: 'ok', reviewerId: 'local', verifiedAt: 'now' }] }, 'c').ready).toBe(true); }); });
