import { describe, expect, it } from 'vitest';
import { claimProvenance, type EvidenceSynthesis } from '@abnt/evidence-synthesis';
const synthesis: EvidenceSynthesis = { version: 1, sources: [], strategies: [], runs: [], inbox: [], records: [], works: [{ id: 'w', title: 'Estudo', recordIds: [], artifactIds: ['a'], fullText: 'available' }], artifacts: [{ id: 'a', workId: 'w', kind: 'pdf' }], evidenceItems: [{ id: 'c', workId: 'w', label: 'Claim', kind: 'claim' }, { id: 'e', workId: 'w', label: 'Achado', kind: 'finding' }], stages: [], criteria: [], decisions: [], extractions: [{ id: 'x', evidenceItemId: 'e', fieldId: 'result', value: 'sim', artifactId: 'a', page: 4, annotationId: 'ann', reviewerId: 'local', verifiedAt: '2026-01-01' }], claimRelations: [{ id: 'r', claimId: 'c', evidenceItemId: 'e', relation: 'supports', reviewerId: 'local', createdAt: '2026-01-01' }] };
describe('CI.8 provenance', () => { it('consolida a cadeia da claim', () => { const result = claimProvenance(synthesis, 'c'); expect(result[0]).toMatchObject({ workTitle: 'Estudo', extractionIds: ['x'], artifactIds: ['a'], pages: [4], annotationIds: ['ann'] }); }); });
