import { describe, expect, it } from 'vitest';
import { claimManuscriptDraft, type EvidenceSynthesis } from '@abnt/evidence-synthesis';
const synthesis: EvidenceSynthesis = { version: 1, sources: [], strategies: [], runs: [], inbox: [], records: [], works: [{ id: 'w', title: 'Estudo principal', recordIds: [], artifactIds: [], fullText: 'available' }], artifacts: [], evidenceItems: [{ id: 'c', workId: 'w', label: 'A intervenção funciona', kind: 'claim' }, { id: 'e', workId: 'w', label: 'Resultado observado', kind: 'finding' }], stages: [], criteria: [], decisions: [], extractions: [], claimRelations: [{ id: 'r', claimId: 'c', evidenceItemId: 'e', relation: 'supports', reviewerId: 'local', createdAt: '2026-01-01' }] };
describe('CI.7 claim manuscript', () => { it('produz prévia rastreável sem mutar síntese', () => { expect(claimManuscriptDraft(synthesis, 'c')).toContain('Suportada: Resultado observado (Estudo principal)'); expect(synthesis.evidenceItems).toHaveLength(2); }); });
